import QtQuick
import "../components" as Components

Components.PlaceholderPage {
    objectName: "historyPage"
    pageTitle: "历史记录"
    pageDescription: "这里将承载历史测试记录、检索、报表回看和追溯入口。当前先保留为导航占位页。"
    pageTag: "HISTORY"
}
