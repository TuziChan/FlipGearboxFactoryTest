import QtQuick
import "../components" as Components

Components.PlaceholderPage {
    objectName: "diagnosticsPage"
    pageTitle: "I/O 诊断"
    pageDescription: "这里将承载传感器、执行器和通信状态的在线诊断。当前先保留为导航占位页。"
    pageTag: "DIAGNOSTICS"
}
